import express from "express"; import { corsMiddleware,requestContext } from "./middleware/core.js"; import { errorHandler,notFound } from "./middleware/error.js"; import { v1Router } from "./routes/v1/index.js";
export const app = express(); app.use(requestContext); app.use(corsMiddleware); app.use(express.json({limit:"512kb"})); app.use("/api/v1",v1Router); app.use(notFound); app.use(errorHandler);
