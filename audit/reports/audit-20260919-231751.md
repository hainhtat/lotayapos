# Finance Refactor Audit Correction Addendum

**Timestamp:** 2026-09-19 23:17:51 +0630 (Asia/Yangon)  
**Supersedes only:** the expense-limit conclusion in `audit-20260919-231638.md`.

## Corrected verification

The expense limit parsing remediation is **confirmed** in the current tree.

`backend/src/controllers/finance.controller.ts:42` now accepts both the number produced by express-validator's `.toInt()` sanitizer and a raw string fallback:

```ts
typeof req.query.limit === "number"
  ? req.query.limit
  : typeof req.query.limit === "string"
    ? Number(req.query.limit)
    : undefined
```

`backend/src/validators/finance/expenses.ts:9` validates the range as 1–200, while `backend/src/services/finance/expenses.ts:44` retains its server-side cap. Static inspection also confirms a regression test in `backend/tests/expense-workflow.test.ts:56-64` for `limit=1` and out-of-range rejection. Per instruction, this addendum did not rerun database tests.

FIN-S02 from the preceding report is withdrawn.

## Updated finding counts

- Critical: 0
- High: 0
- Medium: 1
- Low: 2

The remaining Medium finding is the unexecuted PostgreSQL-only cashbook advisory-lock/concurrency path. Remaining Low observations are rider-outstanding pagination and bounded sequential settlement-chain retrieval.

## Updated deployment recommendation

**Conditional no-go pending isolated PostgreSQL cashbook concurrency verification.** Once that provider-specific test passes, the Finance refactor is acceptable to deploy from this audit's perspective. There is no remaining expense-limit defect.
