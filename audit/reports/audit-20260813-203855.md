# Finance OS + rider filters — follow-up security & money audit

**Timestamp:** 2026-08-13 20:38:55 +0630 (Asia/Yangon)  
**Repository:** `/Users/heinhtetaung/Developer/lotaya-pos`  
**Context:** Working tree after Finance OS pending-return and rider filter/icon work. Prior Critical F-01 (OFFSET not cleared on settlement) was re-checked.

## Prior F-01

**Resolved** on the sequential receive → OS settle path: settlement credits `OS_SETTLEMENT_OFFSET` and reduces net payable.

## Follow-up High findings (addressed in this working tree after the audit)

| ID | Issue | Mitigation implemented |
| --- | --- | --- |
| H-01 | Versioned `OS_RETURN_DEDUCTION` `sourceId` (`parcelId:uuid`) ignored by recoverable/OFFSET sums and hub reversal scope | Sums include `startsWith(parcelId:)`; hub scope uses base parcel id |
| H-02 | Settlement posted preview `returnDeduction` outside serializable tx | Live totals recomputed in Serializable tx; mismatch → `409 BATCH_CHANGED`; `returnDeduction` must cover staged OFFSET |
| H-03 | PARTIAL COD dropped after receive; settle-while-PARTIAL double-recovered advance | RETURNED keeps `actualCodCollected`/fee; PARTIAL OFFSET included in `returnedAdvance`; advance deduction uses unrecovered remainder |
| H-04 | Linked shortfall pro-rata order-dependent; zero-weight last member got remainder | Stable id sort; remainder on last **positive-weight** member |

## Authz / CORS (unchanged conclusion)

- Auditor list-only for pending returns; receive is Finance/Superadmin/OM.
- Rider `dateFrom`/`dateTo` ANDed with `riderId` scope; not an authz bypass.
- CORS allowlist + credentials; Expo `:8081` only in non-production.

## Residual (non-blocking)

- Receive idempotency still note-based (delimiter match tightened).
- Legacy `POST /ledger/return-deductions` still not Serializable.
- Org-wide pending-return list cap 500.
- Production `WEB_ORIGIN` still defaults to localhost if unset.
