# Lotaya POS — Pre-redeploy audit

**Timestamp:** 2026-08-15 13:52 Asia/Yangon  
**Context:** Production live; full review via verifier / test-reviewer / auditor before redeploy.

## Verdict (after remediations in this session)

**Conditional GO** — Urgent High money defects found in the review were fixed and are covered by green suites. Redeploy only after committing the uncommitted money/status fixes into the build artifact.

### Suite results (post-fix)
- Backend: **185/185** passed
- Frontend: **74/74** passed

## Urgent findings (from review) — remediated

### URGENT-01 — Reversed pickup advance could not re-post
- **Was:** `P2002` → false `{ alreadyPosted: true }` after reversal; UI showed Needs posting but ledger stayed reversed.
- **Fix:** versioned `BATCH_PICKUP_ADVANCE` sourceIds via `nextVersionedJournalSourceId`; listBatches / ADVANCE_POSTED / ledger hub scope handle `batchId` and `batchId:…`.
- **Test:** `treats reversed pickup advances as unposted and allows Finance to re-post` — green.

### URGENT-02 — DELIVERED receivable skip after reversal
- **Was:** `recognizeRiderReceivable` short-circuited on recognition row; delivery always used `sourceId: parcel.id`.
- **Fix:** delivery uses `nextRiderReceivableSourceId` before recognition.

### URGENT-03 — Linked commission/fee not re-postable after reversal
- **Fix:** `LINKED_RIDER_COMMISSION`, `LINKED_RIDER_RECEIVABLE_FEE`, `LINKED_RIDER_RECEIVABLE_COD` use versioned sourceIds.

### DeliveryWay OFD → DELIVERED block
- **Fix (uncommitted prior):** create/complete open way when missing or rider-mismatched so ERP can record outcomes.
- **Tests:** `delivery-outcome-without-way.test.ts` — green.

## Remaining non-blocking

| ID | Severity | Note |
| --- | --- | --- |
| Spec deviation | Medium | Manifest dates use `statusHistory` activity, not batch pickup (intentional; sync PROJECT_SPEC) |
| StatusHistory indexes | Medium | Consider composite index for activity-date manifests at scale |
| PDF font cwd | Low | Relies on systemd WorkingDirectory + deploy assets rsync |
| DATE_CHANGE alert API test | Low | Unit covered; no status→alert integration test |

## Deploy checklist

1. Commit all money/status/finance fixes (DeliveryWay, versioned advances, receivables).
2. Confirm `backend/assets/fonts/NotoSansMyanmar-Regular.ttf` is in the commit.
3. Deploy with `deploy.sh` (rsync includes `assets/`).
4. Smoke: Dispatch OFD→DELIVERED; Finance advance post → reverse → re-post; Reports Today; Myanmar PDF.
