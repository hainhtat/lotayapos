import {BadgeCheck,Mail,ShieldCheck,UserRound} from "lucide-react";
import {useTranslation} from "react-i18next";
import {useAuth} from "@/app/auth";

export function ProfilePage(){const {t}=useTranslation();const {user}=useAuth();if(!user)return null;return <div className="mx-auto max-w-3xl"><h1 className="font-display text-3xl font-bold">{t("profile")}</h1><p className="mt-2 text-slate-500">{t("profileDescription")}</p><section className="mt-8 overflow-hidden rounded-2xl bg-white shadow-sm dark:bg-[#181a1d]"><div className="bg-[#101318] p-6 text-white"><div className="grid h-16 w-16 place-items-center rounded-2xl bg-[#1598ef] text-2xl font-bold">{user.name.slice(0,2).toUpperCase()}</div><h2 className="mt-4 font-display text-2xl font-bold">{user.name}</h2><p className="mt-1 text-sm text-slate-400">{t("verifiedAccount")}</p></div><dl className="grid gap-5 p-6 sm:grid-cols-2"><div><dt className="flex items-center gap-2 text-xs font-bold uppercase tracking-wide text-slate-400"><Mail size={15}/>{t("email")}</dt><dd className="mt-2 font-semibold">{user.email}</dd></div><div><dt className="flex items-center gap-2 text-xs font-bold uppercase tracking-wide text-slate-400"><ShieldCheck size={15}/>{t("role")}</dt><dd className="mt-2 font-semibold">{user.role.replaceAll("_"," ")}</dd></div><div><dt className="flex items-center gap-2 text-xs font-bold uppercase tracking-wide text-slate-400"><UserRound size={15}/>{t("userId")}</dt><dd className="mt-2 break-all font-mono text-sm">{user.id}</dd></div><div><dt className="flex items-center gap-2 text-xs font-bold uppercase tracking-wide text-slate-400"><BadgeCheck size={15}/>{t("sessionStatus")}</dt><dd className="mt-2 font-semibold text-[#12a66a]">{t("authenticated")}</dd></div></dl></section></div>}
